spring.application.name=backend

# --- CONEXÃO COM O BANCO (DOCKER) ---
# 1. Mudamos o nome do banco para 'postgres' (que é o padrão do container)
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres

# 2. O usuário padrão
spring.datasource.username=postgres

# 3. A senha que definimos no comando do Docker
spring.datasource.password=admin

spring.datasource.driver-class-name=org.postgresql.Driver

# --- COMPORTAMENTO DO JPA ---
# MUITO IMPORTANTE: Como já criamos as tabelas no DBeaver,
# mudei de 'update' para 'validate' ou 'none' para o Java não tentar
# criar tabelas duplicadas ou bagunçar o que você já fez.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8081
api.security.token.secret=minha-chave-secreta-super-segura-123456


# ============================================================
# CONFIGURAÇÃO OFICIAL MERCADO PAGO - ODONTOPRO LMS
# ============================================================

# Credencial de Acesso Oficial (Produção)
# Nota: Esta chave permite transações reais na conta do John.
mercadopago.access_token=APP_USR-7297845726236891-011818-6b7e3dfaee4d7db8d0f0c40818402ec9-3141373538

# URL de Notificação (Webhook)
# ATENÇÃO: Esta URL deve ser substituída pelo Fernando assim que o deploy for feito.
# Enquanto o sistema estiver em localhost, as notificações não chegarão automaticamente.
mercadopago.webhook.url=https://seu-dominio-final.com/api/webhook